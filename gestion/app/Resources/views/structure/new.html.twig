{% extends 'client.html.twig' %}

{% block stylesheets %}
    {{parent()}}
{% endblock %}

{% block body %}


    {% block coname %}    {{parent()}}  Créer une nouvelle Structure{% endblock %}
    {{parent()}}



    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Créer" class="btn btn-success" />
    {{ form_end(form) }}

   
            <a href="{{ path('structure_index') }}" class="btn btn-warning" >Revenir a la liste</a>
  
{% endblock %}
